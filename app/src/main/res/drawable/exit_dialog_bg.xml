<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="10dp"
    android:shape="rectangle" >

    <!-- 填充的颜色 -->
    <solid android:color="@color/white" />
    <!-- 描边 -->
    <!-- <stroke
        android:width="@dimen/padding_one"
        android:color= "@color/border_front"/> -->
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:radius="8dp" />

    <!-- padding：Button里面的文字与Button边界的间隔 -->
    <!-- <padding
        android:bottom="@dimen/padding_base_rate"
        android:left="@dimen/padding_base_rate"
        android:right="@dimen/padding_base_rate"
        android:top="@dimen/padding_base_rate" /> -->

</shape>
