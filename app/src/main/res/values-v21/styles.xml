<resources>>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!--透明状态栏的style设置 （这是19以上的style）-->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/CustomToolbarHeight</item>
    </style>

    <style name="MMTheme" parent="BaseTheme">
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="CustomToolbarHeight" parent="Widget.AppCompat.Toolbar">
        <item name="android:padding">0dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="actionOverflowButtonStyle">@style/ActionButton.Overflow.More</item>
    </style>

    <!-- menu中三个点的样式 -->
    <style name="ActionButton.Overflow.More" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_launcher</item>
    </style>
    <!--透明状态栏的style设置 （这是19以上的style） end-->

</resources>
